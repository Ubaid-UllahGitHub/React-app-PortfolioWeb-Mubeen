# Redirect all unknown paths to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true

# Serve JavaScript files directly (no redirect)
[[redirects]]
  from = "/static/*"
  to = "/static/:splat"
  status = 200
